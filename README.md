# MUSE Project Page

This directory contains a static project page for MUSE.

Preview locally:

```bash
cd project_page
python3 -m http.server 8017
```

Then open:

```text
http://127.0.0.1:8017/
```

The page has no build step. It can be copied directly into a GitHub Pages repository.
